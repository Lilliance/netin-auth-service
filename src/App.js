import React, { Component } from "react";
import Router from "./Router";

import "./reset.scss";

class App extends Component {
  render() {
    return (
      <main>
        <Router />
      </main>
    );
  }
}

export default App;
